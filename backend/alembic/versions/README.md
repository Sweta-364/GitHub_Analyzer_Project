# Alembic migration files live here.
# Generate the initial migration after setting up your .env:
#
#   cd backend
#   source venv/bin/activate
#   alembic revision --autogenerate -m "init"
#   alembic upgrade head
